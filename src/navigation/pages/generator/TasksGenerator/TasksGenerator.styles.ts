import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  page: {
    paddingHorizontal: 15,
    paddingTop: 75,
    paddingBottom: 12,
  },
  heading: {
    textAlign: 'center',
    marginBottom: 23,
  },
  button: {
    marginTop: 'auto',
  },
});
