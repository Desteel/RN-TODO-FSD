import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 15,
    paddingVertical: 30,
  },
  heading: {
    alignSelf: 'center',
    marginBottom: 25,
  },
  form: {
    rowGap: 16,
    marginBottom: 25,
  },
  input: {
    marginBottom: 16,
  },
  button: {
    marginTop: 'auto',
  },
});
