import { StyleSheet } from 'react-native';
import { shape } from 'shared/theme';

export const styles = StyleSheet.create({
  container: {
    padding: 15,
    paddingTop: 10,
    borderBottomLeftRadius: shape.borderRadius.xlg,
    borderBottomEndRadius: shape.borderRadius.xlg,
  },
});
